general {
    lock_cmd = hyprlock
}

Lock screen after 10 minutes
listener {
    timeout = 600
    on-timeout = hyprlock
}

suspend after 15 minutes
listener {
    timeout = 900
    on-timeout = systemctl suspend
}
